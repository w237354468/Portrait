package com.wzq.search.base;

//import com.mongodb.MongoClient;
//import com.mongodb.ServerAddress;
//import com.wzq.utils.ReadProperties;
//import org.springframework.stereotype.Service;

import java.util.ArrayList;

public class BaseMongo {

//  protected static final MongoClient mongoClient = null;

//    static {
//        //地址
//        ArrayList<ServerAddress> address = new ArrayList<>();
//        String[] addressList = ReadProperties.getKey("mongoaddr", "wzqSearch.properties").split(",");
//        String[] portList = ReadProperties.getKey("mongoport", "wzqSearch.properties").split(",");
//        for (int i = 0; i < addressList.length; i++) {
//            ServerAddress address1 = new ServerAddress(addressList[i], Integer.parseInt(portList[i]));
//            address.add(address1);
//        }
//        mongoClient = new MongoClient(address);
//    }
}
